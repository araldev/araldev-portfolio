# Playwright test results (HTML diff report + raw artifacts)
# Generated by `pnpm test:visual` when a spec fails. Regenerate locally
# with `pnpm exec playwright show-report`; do not commit.
playwright-report/
test-results/
